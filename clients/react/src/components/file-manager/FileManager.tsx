import FileManagerCtrl from './FileManagerCtrl';

interface Props {
}

export default function FileManager(props: Props) {
    const { } = props;

    return (
        <>
            <FileManagerCtrl />
        </>
    );
}